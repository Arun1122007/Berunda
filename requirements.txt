# ── Core ──────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# ── Database ──────────────────────────────────────────────────
sqlalchemy>=2.0.35
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.0
pgvector>=0.2.1

# ── Auth ──────────────────────────────────────────────────────
pyjwt>=2.9.0
bcrypt>=4.0.0

# ── Data Processing ──────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0

# ── Geospatial ───────────────────────────────────────────────
shapely>=2.0.0
geopandas>=1.0.0

# ── Graph ────────────────────────────────────────────────────
networkx>=3.3.0
# neo4j>=5.23.0  # Uncomment for Neo4j graph database integration

# ── Event-driven / Background Tasks ─────────────────────────
celery>=5.4.0
redis>=5.2.0

# ── Rate Limiting ────────────────────────────────────────────
slowapi>=0.1.9

# ── Monitoring / Metrics ────────────────────────────────────
prometheus-client>=0.21.0

# ── ML / AI ──────────────────────────────────────────────────
scikit-learn>=1.5.0
spacy>=3.7.0

# ── Synthetic Data ───────────────────────────────────────────
Faker>=40.35.0

# ── HTTP / API ───────────────────────────────────────────────
requests>=2.32.0
httpx>=0.27.0
slowapi>=0.1.9
presidio-analyzer>=2.2.353
presidio-anonymizer>=2.2.353

# ── Testing ──────────────────────────────────────────────────
pytest>=8.3.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
pytest-asyncio>=0.24.0

# ── Code Quality ─────────────────────────────────────────────
ruff>=0.5.0
mypy>=1.11.0
bandit>=1.7.9
pre-commit>=3.8.0
# Generate lockfile: pip freeze > requirements.lock

# ── Documentation / Diagrams (optional) ──────────────────────
# mkdocs, mermaid-cli — install as needed

aiomysql>=0.2.0
cryptography>=41.0.0
zcatalyst-sdk>=1.0.0

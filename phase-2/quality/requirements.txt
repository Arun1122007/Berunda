# Quality workstream Python dependencies
# Install: pip install -r phase-2/quality/requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
httpx>=0.27.0
asyncio>=3.4.3

# Linting & formatting
ruff>=0.5.0
mypy>=1.11.0

# Security scanning
bandit>=1.7.9

# Dependency checks
packaging>=23.0

# Secret scanning
gitleaks>=8.18.0  # note: requires binary install separately

# Pre-commit
pre-commit>=3.8.0

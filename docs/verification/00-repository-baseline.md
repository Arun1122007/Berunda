# 00 Repository Baseline

## General Information
- **Repository Root**: `D:\Hack2Skill\Berunda`
- **Date**: 2026-07-26
- **Status**: Codebase is currently configured for a mix of SQLAlchemy (local SQLite) and Catalyst ZCQL (Staging/Production).

## Discovered Application Structure

### Backend
- **Path**: `src/`
- **Active Router**: FastAPI (`src/main.py`)
- **Database Abstraction**: `src/repositories/base.py`, `src/repositories/catalyst_adapter.py`, `src/repositories/sqlite_adapter.py`.
- **AppSail Build Artifact Folder**: `appsail/berunda_api/` (generated by `scripts/build_appsail.ps1`)

### Frontend
- **Active Frontend Path**: `apps/web/`
- **Framework**: React + Vite + TypeScript + Tailwind CSS

### Obsolete / Duplicate Folders
- **`Drishti-Crime-Viz/`**: Contains a duplicate React app (Replit artifact). Must not be deployed.
- **`apps/api/`**: Contains an obsolete Node.js API.
- **`berunda.db`**: Exists in multiple locations (`/berunda.db`, `src/berunda.db`, `apps/api/berunda.db`).

### Configuration Files
- `.env`, `.env.example`, `.env.production`
- `catalyst.json`: Mentions `apps/web/dist` and `appsail/berunda_api`.
- `requirements.txt`: Python dependencies (aiomysql was removed in previous iterations).
